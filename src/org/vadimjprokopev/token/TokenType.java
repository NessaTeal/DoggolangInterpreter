package org.vadimjprokopev.token;

public enum TokenType {
    SET,
    ADD,
    SUBTRACT,
    MULTIPLY,
    LESS,
    MORE,
    IF,
    THEN_IF,
    ELSE,
    END_IF,
    WHILE,
    THEN_WHILE,
    END_WHILE,
    VARIABLE,
    CONSTANT,
    EOF
}
